
const Studio = () => {
  return (
    <main className="relative p-8">
        <p className="text-muted text-center">Studio (the creative kind, not the recording kind)</p>
    </main>
  );
};

export default Studio;
